import { SeatTypesValidators } from './seat-types-validators';

describe('SeatTypesValidators', () => {
  it('should create an instance', () => {
    expect(new SeatTypesValidators()).toBeTruthy();
  });
});
